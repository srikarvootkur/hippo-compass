# Mac iMessage Bridge

iMessage access belongs on a trusted Mac, not the Hetzner server.

The bridge should:

- run locally on macOS
- use explicit conversation allowlists
- create writing-style summaries and draft context
- queue reply drafts for approval
- avoid autonomous sending in v1

Raw message content should stay local unless explicitly approved for sync.
